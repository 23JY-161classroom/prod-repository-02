# 不正なパスワードが設定されてしまうと、バリデーションが走ってログイン不能になってしまう恐れがある
# この事故を防ぐため、CurrentPassword / NewPassword で分割している
type: string
description: ユーザーがログインに使用するパスワード。空白を除く全てのASCII印刷可能文字を指定可能。
pattern: "^[\x21-\x7E]+$"
minLength: 8
maxLength: 100
example: p@ss0rd123!v