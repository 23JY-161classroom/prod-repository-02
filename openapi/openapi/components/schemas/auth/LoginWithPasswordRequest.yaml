type: object
properties:
  username: 
    type: string
    description: ログイン時に用いるユーザーの識別子。現状は仕様通りメールアドレスを用いるが、将来拡張される可能性がある。
    example: "user01@example.com"
  password:
    $ref: '../common/primitives/CurrentPassword.yaml'
required: 
  - username
  - password
