type: object
properties:
  current_password: 
    description: 現在のパスワード
    example: old
    $ref: '../common/primitives/CurrentPassword.yaml'
  new_password:
    description: 新しく設定するパスワード
    $ref: '../common/primitives/NewPassword.yaml'
  new_password_verify:
    description: 新しく設定するパスワード（確認用）
    $ref: '../common/primitives/NewPassword.yaml'